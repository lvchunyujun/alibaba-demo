server:
  port: 8881
  servlet:
    context-path: /api
spring:
  application:
    name: my-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848
